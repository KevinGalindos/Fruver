const pool = require('../database/database');
const session = require('express-session');
const UserData = {};



UserData.Load_Menu = async(userid, companyid) => {

    /**
     * this method get the session information  
     * 
     * @return {Array in the session} 
     */


}



module.exports = UserData;